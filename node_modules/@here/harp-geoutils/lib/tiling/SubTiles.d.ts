import { TileKey } from "./TileKey";
export declare class SubTiles implements Iterable<TileKey> {
    tileKey: TileKey;
    sizeX: number;
    sizeY: number;
    constructor(tileKey: TileKey, sizeX: number, sizeY: number);
    [Symbol.iterator](): Iterator<TileKey>;
}
export declare namespace SubTiles {
    function RowColumnIterator(parentKey: TileKey, sizeX: number, sizeY: number): Iterator<TileKey>;
    function ZCurveIterator(parentKey: TileKey): Iterator<TileKey>;
}
//# sourceMappingURL=SubTiles.d.ts.map