import { GeoJson, ITiler } from "@here/harp-datasource-protocol";
import { TileKey } from "@here/harp-geoutils";
interface GeoJsonVtIndex {
    geojson: GeoJson;
    getTile(level: number, column: number, row: number): any;
}
export declare class GeoJsonTiler implements ITiler {
    indexes: Map<string, GeoJsonVtIndex>;
    constructor();
    dispose(): void;
    connect(): Promise<void>;
    registerIndex(indexId: string, input: URL | GeoJson): Promise<void>;
    updateIndex(indexId: string, input: URL | GeoJson): Promise<void>;
    getTile(indexId: string, tileKey: TileKey): Promise<{}>;
    private getOriginalGeometry;
}
export {};
//# sourceMappingURL=GeoJsonTiler.d.ts.map